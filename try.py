import extract_events, pdb, sys, gensim
from gensim import corpora, models, similarities
from type_arguments import *
import cPickle as pickle

rep = pickle.load(open('repository_debug','rb'))
pdb.set_trace()
